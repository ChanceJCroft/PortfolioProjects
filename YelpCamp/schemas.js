const Joi = require('joi');

//Joi allows for server side validation, rather than relying on just client side validation

//validating campgrounds to ensure they have all data needed
module.exports.campgroundSchema = Joi.object({
    campground: Joi.object({
        title: Joi.string().required(),
        price: Joi.number().required().min(0),
        image: Joi.string().required(),
        location: Joi.string().required(),
        description: Joi.string().required()
    }).required(),
});

//validating reviews - make sure it has a review text body and a rating
module.exports.reviewSchema = Joi.object({
    review: Joi.object({
        rating: Joi.number().required().min(1).max(5),
        body: Joi.string().required()
    }).required()
})